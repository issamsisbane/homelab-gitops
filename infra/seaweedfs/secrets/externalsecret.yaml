apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: seaweedfs-s3-secret
  namespace: seaweedfs
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: openbao-store
    kind: SecretStore
  target:
    name: seaweedfs-s3-secret
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        seaweedfs_s3_config: |
          {
            "identities": [
              {
                "name": "loki",
                "credentials": [
                  {
                    "accessKey": "{{ .loki_accessKey }}",
                    "secretKey": "{{ .loki_secretKey }}"
                  }
                ],
                "actions": [
                  "Read",
                  "Write",
                  "List",
                  "Tagging"
                ]
              }
            ]
          }
 
  data:
    - secretKey: accessKey
      remoteRef:
        key: seaweedfs/s3-credentials
        property: loki_accessKey
 
    - secretKey: secretKey
      remoteRef:
        key: seaweedfs/s3-credentials
        property: loki_secretKey
