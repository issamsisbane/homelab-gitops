apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: openbao-store
  namespace: seaweedfs
spec:
  provider:
    vault:
      server: "http://openbao.openbao.svc.cluster.local:8200"
      path: "seaweedfs"       # le KV mount créé par OpenTofu
      version: "v2"
      auth:
        kubernetes:
          mountPath: "kubernetes"
          role: "role-seaweedfs"          # créé par OpenTofu
          serviceAccountRef:
            name: "sa-vault-seaweedfs"    # le SA ci-dessus
