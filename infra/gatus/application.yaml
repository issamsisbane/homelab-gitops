apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: gatus
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: infra

  source:
    repoURL: https://twin.github.io/helm-charts
    chart: gatus
    targetRevision: 1.5.0

    helm:
      values: |
        env:
          MATRIX_ACCESS_TOKEN:
            valueFrom:
              secretKeyRef:
                name: gatus-matrix
                key: access-token
          MATRIX_STATUS_ROOM_ID:
            valueFrom:
              secretKeyRef:
                name: gatus-matrix
                key: status-room-id
          DB_HOST:
            valueFrom:
              secretKeyRef:
                name: gatus-cnpg-app
                key: host
          DB_PORT:
            valueFrom:
              secretKeyRef:
                name: gatus-cnpg-app
                key: port
          DB_NAME:
            valueFrom:
              secretKeyRef:
                name: gatus-cnpg-app
                key: dbname
          DB_USER:
            valueFrom:
              secretKeyRef:
                name: gatus-cnpg-app
                key: username
          DB_PASSWORD:
            valueFrom:
              secretKeyRef:
                name: gatus-cnpg-app
                key: password

        config:
          alerting:
            matrix:
              server-url: "https://matrix.issamhomelab.org"
              access-token: "${MATRIX_ACCESS_TOKEN}"
              internal-room-id: "${MATRIX_STATUS_ROOM_ID}"
          ui:
            default-sort-by: "group"
            header: ""
            title: "Status - Issamhomelab"
            logo: ""
            dashboard-subheading: "Endpoints status in realtime"
          storage:
            type: postgres
            path: "postgres://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}?sslmode=require"
          endpoints:
            - name: Zigbee2MQTT
              group: Apps
              url: "http://zigbee2mqtt.domotic.svc.cluster.local:8080"
              interval: 30s
              conditions:
                - "[STATUS] == 200"
                - "[RESPONSE_TIME] < 300"
            - name: Microbin
              group: Apps
              url: "https://bin.issamhomelab.org"
              interval: 30s
              conditions:
                - "[STATUS] == 200"
                - "[RESPONSE_TIME] < 300"
            - name: authelia
              group: Infra
              url: "https://auth.issamhomelab.org"
              interval: 30s
              conditions:
                - "[STATUS] == 200"
                - "[RESPONSE_TIME] < 300"
            - name: argocd
              group: Infra
              url: "http://my-argo-cd-argocd-server.argocd.svc.cluster.local"
              interval: 30s
              conditions:
                - "[STATUS] == 200"
                - "[RESPONSE_TIME] < 300"
            - name: homepage
              group: Infra
              url: "https://home.issamhomelab.org"
              interval: 30s
              conditions:
                - "[STATUS] == 401"
                - "[RESPONSE_TIME] < 300"
            - name: openbao
              group: Infra
              url: "http://openbao.openbao.svc.cluster.local:8200"
              interval: 30s
              conditions:
                - "[STATUS] == 200"
                - "[RESPONSE_TIME] < 300"
            - name: grafana
              group: Infra
              url: "http://grafana-service.observability.svc.cluster.local:3000"
              interval: 30s
              conditions:
                - "[STATUS] == 200"
                - "[RESPONSE_TIME] < 300"
            - name: headlamp
              group: Infra
              url: "https://headlamp.issamhomelab.org"
              interval: 30s
              conditions:
                - "[STATUS] == 200"
                - "[RESPONSE_TIME] < 300"
            - name: MASTER-1
              group: Machines
              url: "tcp://ih-node-34:22"
              interval: 30s
              conditions:
                - "[CONNECTED] == true"
                - "[RESPONSE_TIME] < 200"
              alerts:
                - type: matrix
                  description: "Master-1 is down.."
                  send-on-resolved: true
            - name: WORKER-1
              group: Machines
              url: "tcp://ih-node-1:22"
              interval: 30s
              conditions:
                - "[CONNECTED] == true"
                - "[RESPONSE_TIME] < 200"
            - name: WORKER-2
              group: Machines
              url: "tcp://192.168.1.16:22"
              interval: 30s
              conditions:
                - "[CONNECTED] == true"
                - "[RESPONSE_TIME] < 200"

        persistence:
          enabled: false

  destination:
    server: https://kubernetes.default.svc
    namespace: gatus

  syncPolicy:
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
    automated:
      prune: true
      selfHeal: true
