apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: openbao
  namespace: openbao
spec:
  defaultBackend:
    service:
      name: openbao
      port:
        name: http
  ingressClassName: tailscale
  tls:
  - hosts:
    - openbao
