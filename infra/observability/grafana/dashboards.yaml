apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: node-exporter-full
  namespace: observability
spec:
  instanceSelector:
    matchLabels:
      dashboards: grafana
  folder: "Kubernetes"
  url: "https://grafana.com/api/dashboards/1860/revisions/37/download"
  datasources:
    - inputName: "DS_PROMETHEUS"
      datasourceName: "Prometheus"

---
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: k8s-compute-resources-cluster
  namespace: observability
spec:
  instanceSelector:
    matchLabels:
      dashboards: grafana
  folder: "Kubernetes"
  url: "https://grafana.com/api/dashboards/7249/revisions/1/download"
  datasources:
    - inputName: "DS_PROMETHEUS"
      datasourceName: "Prometheus"

---
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: k8s-compute-resources-namespace
  namespace: observability
spec:
  instanceSelector:
    matchLabels:
      dashboards: grafana
  folder: "Kubernetes"
  url: "https://grafana.com/api/dashboards/7250/revisions/1/download"
  datasources:
    - inputName: "DS_PROMETHEUS"
      datasourceName: "Prometheus"

---
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: k8s-compute-resources-pod
  namespace: observability
spec:
  instanceSelector:
    matchLabels:
      dashboards: grafana
  folder: "Kubernetes"
  url: "https://grafana.com/api/dashboards/6417/revisions/1/download"
  datasources:
    - inputName: "DS_PROMETHEUS"
      datasourceName: "Prometheus"

---
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: alertmanager
  namespace: observability
spec:
  instanceSelector:
    matchLabels:
      dashboards: grafana
  folder: "Kubernetes"
  url: "https://grafana.com/api/dashboards/9578/revisions/4/download"
  datasources:
    - inputName: "DS_PROMETHEUS"
      datasourceName: "Prometheus"

---
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: loki-logs
  namespace: observability
spec:
  instanceSelector:
    matchLabels:
      dashboards: grafana
  folder: "Logs"
  url: "https://grafana.com/api/dashboards/13639/revisions/2/download"
  datasources:
    - inputName: "DS_LOKI"
      datasourceName: "Loki"
