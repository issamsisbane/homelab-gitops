apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: grafana-cnpg
  namespace: observability
spec:
  instances: 2
  storage:
    size: 10Gi

  bootstrap:
    initdb:
      database: grafana
      owner: grafana
