apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: openbao-store
  namespace: observability
spec:
  provider:
    vault:
      server: "http://openbao.openbao.svc.cluster.local:8200"
      path: "observability"
      version: "v2"
      auth:
        kubernetes:
          mountPath: "kubernetes"
          role: "role-observability"
          serviceAccountRef:
            name: "sa-vault-observability"
