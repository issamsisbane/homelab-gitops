apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: grafana
  namespace: grafana
spec:
  defaultBackend:
    service:
      name: grafana-service
      port:
        name: grafana
  ingressClassName: tailscale
  tls:
  - hosts:
    - grafana
