apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: authentik-cnpg
  namespace: default
spec:
  instances: 2
  storage:
    size: 10Gi

  bootstrap:
    initdb:
      database: authentik
      owner: authentik
