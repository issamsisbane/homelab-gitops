apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: authentik-cnpg
  namespace: authentik
spec:
  instances: 2
  storage:
    size: 10Gi

  bootstrap:
    initdb:
      database: authentik
      owner: authentik
