apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: longhorn
  namespace: longhorn
spec:
  defaultBackend:
    service:
      name: longhorn-frontend
      port:
        name: 8000
  ingressClassName: tailscale
  tls:
  - hosts:
    - longhorn
