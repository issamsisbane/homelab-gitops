apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: zipline-cnpg
  namespace: zipline
spec:
  instances: 2
  storage:
    size: 1Gi

  bootstrap:
    initdb:
      database: zipline
      owner: zipline
