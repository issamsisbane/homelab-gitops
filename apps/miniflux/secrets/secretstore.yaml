apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: openbao-store
  namespace: miniflux
spec:
  provider:
    vault:
      server: "http://openbao.openbao.svc.cluster.local:8200"
      path: "miniflux"       # le KV mount créé par OpenTofu
      version: "v2"
      auth:
        kubernetes:
          mountPath: "kubernetes"
          role: "role-miniflux"          # créé par OpenTofu
          serviceAccountRef:
            name: "sa-vault-miniflux"    # le SA ci-dessus
