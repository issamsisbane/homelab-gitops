apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: readeck-cnpg
  namespace: readeck
spec:
  instances: 2
  storage:
    size: 10Gi

  bootstrap:
    initdb:
      database: readeck
      owner: readeck
